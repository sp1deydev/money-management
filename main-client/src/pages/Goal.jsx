import React from 'react';
import PropTypes from 'prop-types';

Goal.propTypes = {
    
};

function Goal(props) {
    return (
        <div>
            Goal Page
        </div>
    );
}

export default Goal;