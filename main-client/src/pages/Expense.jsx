import React from 'react';
import PropTypes from 'prop-types';

Expense.propTypes = {
    
};

function Expense(props) {
    return (
        <div>
            Expense Page
        </div>
    );
}

export default Expense;